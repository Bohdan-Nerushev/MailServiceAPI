apiVersion: 1

contactPoints:
  - name: Email
    receivers:
      - uid: email-receiver-1
        type: email
        settings:
          addresses: admin@example.com
          singleEmail: true

policies:
  - orgId: 1
    receiver: Email
    group_by: ['alertname', 'severity']
    repeat_interval: 4h
